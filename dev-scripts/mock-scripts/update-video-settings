#!/bin/bash

# Mock version of /opt/basslinemnk-privileged/update-video-settings

echo "Starting mock version of update-video-settings..."

sleep 3

echo "Mock update video settings succeeded (no changes were applied)"
